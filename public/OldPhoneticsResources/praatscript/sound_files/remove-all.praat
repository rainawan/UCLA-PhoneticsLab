## Praat script by Kevin Ryan 9/05
## Select all objects and remove them from the object list

pause Remove all objects (does not delete any files, but unsaved objects will be lost)?
select all
Remove
